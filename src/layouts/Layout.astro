---
import "../styles/global.css";
import "@fontsource/nunito-sans";
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="generator" content={Astro.generator} />
    <title>Aluxez Administración</title>
    <link rel="icon" type="image/svg+xml" href="/favicons/96px.svg" />
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css" />
    <style>
      /* Ocultar elementos de debugging y herramientas de desarrollo */
      [class*="debug"], [class*="inspect"], [class*="dev-tool"] {
        display: none !important;
      }
      .debug-overlay, .css-inspector, .tooltip-debug, .dev-tools-tooltip {
        display: none !important;
      }
      /* Estilos para el overlay y blur cuando el menú está activo */
      body.menu-open {
        overflow: hidden !important; /* Deshabilita el scroll del body */
        position: fixed !important; /* Fija la posición del body */
        width: 100%; /* Asegura que el body ocupe todo el ancho */
        left: 0;
        right: 0;
        user-select: none !important; /* Evita la selección de texto */
        -webkit-user-select: none !important;
        -moz-user-select: none !important;
        -ms-user-select: none !important;
      }
      
      html.menu-open {
        overflow: hidden !important;
      }
      
      #page-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh;
        background-color: rgba(0, 51, 50, 0.5);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        z-index: 45; /* Aseguramos que esté por encima del contenido pero por debajo del menú */
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.3s ease;
        visibility: hidden;
      }
      
      body.menu-open #page-overlay,
      #page-overlay.active {
        opacity: 1;
        pointer-events: auto; /* Capturar clics pero sin cerrar el menú */
        visibility: visible;
        cursor: default; /* Mostrar cursor normal */
      }
      
      /* Estilos para pantallas pequeñas */
      @media (max-height: 600px) {
        #menu-dropdown {
          top: 60px !important;
          max-height: calc(100vh - 70px) !important;
        }
      }
      
      /* Clase que se aplica para pantallas muy pequeñas */
      #menu-dropdown.small-screen {
        top: 60px !important;
      }
    </style>
  </head>
  <body>
    <div id="page-overlay"></div>
    <div id="main-content">
      <slot />
    </div>
  </body>
</html>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;700&display=swap');
  * {
    /*box-sizing: border-box;
    margin: 0;
    padding: 0;*/
    font-family: 'Nunito Sans', sans-serif;
  }
  html{
    overflow: auto;
  }
  body {
    width: 100%;
    height: auto;
    overflow-x: hidden; /* Permitir scroll vertical por defecto, pero prevenir horizontal */
    background: rgb(0, 102, 99);
    /*background: linear-gradient(
      344deg,
      rgba(0, 102, 99, 1) 0%,
      rgba(0, 145, 145, 1) 50%,
      rgba(0, 51, 50, 1) 100%
    );*/
    transition: filter 0.3s ease;
  }
</style>
